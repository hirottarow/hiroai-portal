# 作業前に必ず読むこと

**どんな質問でも、回答・作業の前に必ず `C:\Antigravity\CLAUDE.md` を読むこと。**
**一般的な Android / ADB / git の知識で答えられると思った時ほど読むこと。**
この環境はビルド・実機操作・スクリーンショット・git・ファイルの置き場のいずれも標準手順と違う。
**一般知識で即答すると間違える。読まずに答えないこと。**

ここは `C:\Antigravity` 配下の**独立リポジトリ**。このフォルダを単独で開くと、
親（`C:\Antigravity`）の入口が探索範囲の外に出て届かないため、2026-08-13 にここにも置いた。
同じ理由で `.agents/rules/00-workspace-rules.md`（Antigravity IDE 用の入口）も置いてある。

**このファイルにルールを複製しないこと。** 複製すると片方だけ更新されて腐る
（2026-08-03 に `RULES.md` で実害が出て削除済み）。足す内容は `CLAUDE.md` に書く。
